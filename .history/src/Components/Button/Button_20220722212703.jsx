import React from "react";

const Button = (props) => {
    const { classStyle, handleOnClick } = props;
    return (
        <>
            <button></button>
        </>
    );
};

export default Button;
