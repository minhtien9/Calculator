import { PLUS } from "./type";

const INITIAL_STATE = {
    preState: "",
    curState: "",
    input: 0,
    operator: null,
    total: false,
};

const reducer = (state = INITIAL_STATE,action) => {};
